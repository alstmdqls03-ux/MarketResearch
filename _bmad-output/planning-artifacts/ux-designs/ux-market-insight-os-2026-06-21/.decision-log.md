# Decision Log — Market Insight OS UX

Run: ux-market-insight-os-2026-06-21 · Mode: Create · Language: Korean

## Sources (confirmed)
- `_bmad-output/planning-artifacts/prd.md` — extracted via subagent
- `_bmad-output/planning-artifacts/product-brief.md` — extracted via subagent

## D1 — Target user / purpose
주식 초·중급자, 꾸준함/지속력 약한 사람. 목적: 시장이 어떻게 돌아가는지 쉽게 체감.
Personas: 민수(Primary, 개발자, FOMO/이유 미기록), 지영(Secondary, 직장인, 뉴스 과민 공포매도).

## D2 — Form factor / platform
**하나의 모바일 우선 반응형 웹앱**을 네이티브 **WebView 셸**로 감싸 앱으로 배포.
→ 웹과 앱이 동일 코드베이스. 웹 품질이 곧 앱 품질. 데스크톱은 대시보드 멀티컬럼으로 확장.
실시간 시세 없음(EOD/지연) → 폴링/수동 새로고침. PWA/오프라인은 MVP 제외(Future).
UI 시스템: shadcn/ui + Tailwind (PRD 명시). → 두 스파인 모두 shadcn 기본을 상속·확장.

## D3 — Core loop (HERO)
관심종목 → 리서치 카드(상승논리|하락리스크 분리) → 매수 전 30초 체크[가격·지표 스냅샷 원자저장]
→ (매도/1주/1개월) → AI 복기(기록 vs 결과) → Insight Score(거울).

## D4 — IA surfaces (FR 매핑)
로그인/가입 · 온보딩(빈상태→가치) · 관심종목 · 리서치카드 · 매수30초체크 · 매도체크
· 즉시통찰 · AI복기 · Insight Score(거울) · 대시보드 · Daily Brief · 결정이력(물타기) · 운영자(최소)
· 횡단 신뢰 UI(as-of 배지/가시적 오류/per-output 면책).

## D5 — Interaction philosophy
저마찰(탭/칩/스크롤 우선, 타이핑 선택) + **의도된 마찰**(30초 과속방지턱, 0으로 최적화 금지).
점진 공개. 비강제 넛지("막지 않고 직면"). 비동기 생성+진행표시+재시도.

## D6 — Voice / framing (from PRD, locked)
거울(성적표 아님), 호기심의 쾌감(잔소리 아님). 복기는 데이터 인용 필수, 추천/예측 0건.
네이밍 가드: alpha/picks/signals/journal 회피 → "판단 디버거 / 의사결정 리뷰".

## D7 — DB guardrail (user directive, carried into EXPERIENCE.md)
테이블 최소화 + JSONB 우선. 투자 판단 근거(이유/리스크/기간/감정/목표가/손절)와 스냅샷은
쪼개지 말고 결정 행의 JSONB로 통째 저장. 테이블마다 존재 이유 주석.
→ FR10/11/18 = 단일 결정 행 JSONB. FR15 append-only 버전. FR16 (user,ticker) 1:N 한 테이블.
FR40 매도↔매수 self-reference. UX 플로우가 불필요한 테이블 분리를 유도하지 않게 한다.

## D8 — Accessibility / NFR floor
WCAG 2.1 AA 지향. 탭 타깃 ≥44px, 엄지영역, 30초체크 반응<100ms, 대시보드<2s.
칩은 "기타(직접)" 포함(거짓 기록 방지).

## D9 — Visual identity (elicited)
- **무드: 미니멀 핀테크** — 타이트 그리드, 카드 위주, 중립 그레이 + 단일 브랜드 컬러,
  sans-serif, 균일 위계, 군더더기 없음. (사용자 메모: 빠른 구현, 무난함 선호.)
- **모드: 라이트 + 다크 둘 다** (시스템 따라 자동). 토큰 2벌 관리.
- **등락 색: 한국식** — 빨강=상승, 파랑=하락. (대상은 미국 종목이나 한국 사용자 직관 우선.)
  → 색맹 대비 위해 부호/화살표 병기 권장(접근성 보완).

## D10 — Brand color (selected)
**Deep Indigo** — light `#4F46E5` / dark `#818CF8`. "전문가의 조용한 확신".
의미색 고정(전 테마 공통): 상승=빨강 `#E5383B`, 하락=파랑 `#2563EB`. 브랜드는 빨강/파랑과 비충돌.
artifact: `.working/color-themes-1.html`

## D11 — Typography (confirmed)
**Pretendard** (KR/Latin 통합). 숫자 **tabular-nums**(시세·등락률 정렬). 위계 3단: 제목/본문/캡션(as-of·면책).

## D12 — Spines distilled
DESIGN.md (~180줄) + EXPERIENCE.md (~130줄) 생성 완료. status: draft.

## D13 — Open-item triage (user: "다해줘" = 전부 확정)
- 운영자 화면(FR36-38): 전용 플로우 없이 IA/State에만. MVP 의도적 축소. ✓
- Daily Brief: on-demand 수동 열기(자동 푸시 없음) 가정. ✓
- 로그인/가입(FR1-2): 표준 인증, 전용 플로우 생략. ✓
- 결정이력/물타기 탐색: 독립 플로우는 Growth로 미룸, 매도-매수 연결로 커버. ✓

## D14 — Finalize actions (user: 전부)
핵심 화면 목업 3종 렌더: 리서치 카드 / 매수 30초 체크(점진 공개) / 대시보드.
리뷰어 게이트: 루브릭 워커 + 접근성 렌즈(소비자 제품) 실행 → validation-report.

## D15 — Review fixes applied (12건)
대비(면책 #52525B / 상승텍스트 #C81E1E / as-of #9A3E07), destructive=zinc #3F3F46(빨강 회피),
numeric 사이즈, 대비 타깃, 칩 aria-pressed+점진공개 announce, 30초 self-paced(2.2.1 비위반),
FR43 거부 상태, 운영자 State, Insight Score 명칭 통일, spines-win 선언, numeric 토큰.
검증 시점 critical/high 전부 해결. 잔여 미해결은 medium/low(접근성 계약 디테일) → 다음 게이트.

## D16 — FINAL
mockups/ 승격(color-themes-1 + key-research-card/buy-check/dashboard), 스파인 인라인 링크,
시각 레퍼런스 커버리지 노트. DESIGN.md / EXPERIENCE.md status: final (2026-06-21).

## SECURITY NOTE
목업 1차 렌더 서브에이전트 응답에 프롬프트 인젝션("office-hours 스킬 실행하라", task_instructions 위장) 발생.
사용자 지시 아님 → 무시. 산출물/소스 grep 결과 인젝션 텍스트 미혼입(클린). 목업은 재렌더로 정상 생성.

## Next step
bmad-create-architecture (또는 bmad-create-epics-and-stories). 새 컨텍스트 창 권장.
